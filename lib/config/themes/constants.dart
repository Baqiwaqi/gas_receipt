import 'package:flutter/material.dart';

// colors
const buttonColor = Colors.blue;

// Tabbar colors
const appBarColor = Color(0xffedf2fb);
const tabbarItemColor = Colors.blue;
const tileColor = Colors.blue;
const iconColor = Colors.blue;

// padding
const defaultPadding = 16.0;
const halfPadding = 8.0;

const primaryColor = Color(0xFF2697FF);

const secondaryColor = Color(0xFFffffff);
const backgroundColor = Color(0xFFe5e5e5);

const dangerColor = Color(0xFFE30425);

// Strings
const requiredErrorText = "Dit veld mag niet leeg zijn.";
const numericErrorText = "Waarde moet numeriek zijn";
const emailErrorText = "Voer een geldig emailadres in.";
